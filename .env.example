# ===========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ===========================================
# Copia este archivo como .env y ajusta los valores según tu entorno

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ===========================================

# Opción 1: URL completa de la base de datos (recomendado para producción)
# DATABASE_URL=postgresql://usuario:contraseña@host:puerto/nombre_bd
DATABASE_URL=postgresql://postgres:Ho2025@localhost:5432/mental_health_app

# Opción 2: Variables individuales (como respaldo si no se usa DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=Ho2025
DB_NAME=mental_health_app

# ===========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ===========================================

# Puerto en el que se ejecutará la aplicación
PORT=3000

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# Clave secreta para JWT (¡CAMBIA ESTE VALOR EN PRODUCCIÓN!)
JWT_SECRET=your-secret-key-change-in-production

# ===========================================
# CONFIGURACIÓN DE SERVICIOS DE IA
# ===========================================

# Configuración de Ollama
OLLAMA_API_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:0.5b

# ===========================================
# NOTAS DE CONFIGURACIÓN
# ===========================================

# Para desarrollo local:
# - Usa las configuraciones por defecto mostradas arriba
# - Asegúrate de tener PostgreSQL ejecutándose localmente
# - O usa Docker Compose que incluye PostgreSQL

# Para producción:
# - Cambia JWT_SECRET por una clave segura y única
# - Usa una contraseña fuerte para la base de datos
# - Considera usar variables de entorno del sistema en lugar de un archivo .env

# Para Docker Compose:
# - Las variables se tomarán del archivo .env automáticamente
# - El servicio de PostgreSQL se ejecutará en el contenedor 'postgres'
# - DATABASE_URL se ajustará automáticamente para usar el contenedor
